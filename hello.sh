#! /usr/bin/bash

# The above line will tell the interpreter that this is a bash script & it is a mandatory step that we need to write it. The meaning of (#!) is shebang or hashbang then the location of the bash.

echo "hello world" # After echo whatever is written will be printed on the terminal